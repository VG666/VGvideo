# 开源声明 / Open Source Notices

> 本文件仅针对 **VG视频 桌面客户端**（本项目本体：`main.py` + `model/` `view/` `controller/` `utility/` `config/` `test/` 等）。
> `tx_server/`（腾讯视频取流服务）与 `test_server/`（本地测试源）是**独立项目**，仅临时置于本目录下，不在本声明范围内，其许可证随各自项目另附。
>
> 下文列出客户端运行/构建所依赖的第三方开源软件。若你随安装包分发本程序，请按各组件许可证要求一并附上对应许可证全文（copyleft 组件尤其如此）。

---

## 一、组件清单

| 组件 | 版本 | 许可证 | 版权方 / 作者 | 用途 | 是否随包分发 |
| --- | --- | --- | --- | --- | --- |
| **Python** | 3.8+（已验证 3.11 / 3.14） | [PSF License Agreement](https://www.python.org/download/releases/3.11/license/) | Python Software Foundation | 运行语言 / 解释器 | 是（由 Nuitka 打包进 exe） |
| **Tcl / Tk**（Tkinter 后端） | 随 Python 发行 | [Tcl/Tk BSD 风格许可证](https://www.tcl.tk/software/tcltk/license.html) | Tcl Core Team / Scriptics 等 | GUI 界面底层（Tkinter 封装其上） | 是（见 `build_nuitka/main.dist/tk/license.terms`） |
| **libVLC / VLC**（含 `videovlc/`） | 3.0.11 | 库：**LGPL-2.1+**；应用程序与插件：**GPL-2.0+** | VideoLAN | 音视频播放内核（动态库） | 是（`videovlc/` 目录整体随包） |
| **python-vlc**（`utility/vlc.py`） | 绑定至 libVLC 3.0.11 | **LGPL-2.1+** | VideoLAN team；Olivier Aubert 等 | libVLC 的 Python ctypes 绑定 | 是（`utility/vlc.py`） |
| **Pillow**（PIL） | — | [MIT / HPND](https://pillow.readthedocs.io/en/stable/about.html#license) | Pillow 贡献者 / Fredrik Lundh 等 | 界面图片缩放与处理 | 是（Python 依赖，打包进 exe） |
| **Nuitka** | — | [Apache-2.0](https://www.nuitka.net/doc/user/license.html) | Kay Hayen | 独立可执行文件构建工具（仅构建期，不进运行时） | 否（仅构建环境使用） |
| **aria2c**（可选，`utility/aria2c.exe`） | — | **GPL-2.0+** | Tatsuhiro Tsujikawa | 下载加速（HLS 分片并发抓取）；缺失则回落内置实现 | 可选（放入 `utility/` 才随包） |
| **ffmpeg**（可选，`utility/ffmpeg.exe`） | — | **GPL-2.0+** 或 **LGPL-2.1+**（取决于构建） | FFmpeg team | 下载后的 remux 合成 mp4；缺失则回落内置实现 | 可选（放入 `utility/` 才随包） |
| **windnd**（`utility/windnd.py`） | — | 第三方共享模块（无显式许可证声明，按原作者的宽松共享意图使用） | 社区流传的 Tk 拖拽实现 | Windows 下窗口拖拽打开文件 | 是（`utility/windnd.py`） |
| **ToolTips**（`utility/ToolTips.py`） | — | 第三方共享组件（ActiveState Tkinter 配方衍生，无显式许可证声明） | 社区配方衍生 | Tkinter 气泡提示控件 | 是（`utility/ToolTips.py`） |

---

## 二、关键许可证说明（copyleft 提醒）

- **libVLC（库部分）与 python-vlc 绑定** 为 **LGPL-2.1 或更高版本**。若你对 `videovlc/` 或 `utility/vlc.py` 做了修改并分发，须按 LGPL 提供修改处的源码或差异；动态链接（本项目即通过 ctypes 动态调用 `libvlc.dll`）本身不要求公开你自己的应用源码。
- **VLC 应用程序本体与插件**（`videovlc/vlc.exe`、`plugins/*.dll` 等）为 **GPL-2.0 或更高版本**。随包分发这些二进制即触发 GPL 的传播义务：你必须向接收者提供**对应版本的完整对应源码**（或书面提供获取方式）。
- **aria2c / ffmpeg** 为 **GPL-2.0+**：二者均为**可选**组件，仅在被显式放入 `utility/` 并随包时才需履行 GPL 义务；不放则客户端回落到内置实现，不受影响。
- **Tcl/Tk、Pillow、Python、Nuitka** 为宽松许可证（BSD / MIT / PSF / Apache-2.0），分发时保留原许可证与版权声明即可，无源码公开义务。

各 copyleft 许可证的**官方全文**请见：
- GPL-2.0：<https://www.gnu.org/licenses/old-licenses/gpl-2.0.txt>
- LGPL-2.1：<https://www.gnu.org/licenses/old-licenses/lgpl-2.1.txt>
- Apache-2.0：<https://www.apache.org/licenses/LICENSE-2.0.txt>

---

## 三、本项目自身代码

VG视频 客户端**自身编写的代码**以 **GNU LGPL-3.0** 发布，完整声明与版权信息见仓库根目录的 **`LICENSE`**（LGPL-3.0 正文 `LICENSE.LGPL-3.0.txt`，其引用的 GPL-3.0 正文 `LICENSE.GPL-3.0.txt`）。

> **聚合分发说明**：本仓库随包分发 VLC（含 GPL-2.0+ 的应用程序与插件）、可选的 aria2c/ffmpeg（GPL-2.0+）等强 copyleft 组件。它们以**独立程序 / 独立插件**的形式与本项目一同提供，属于 GPL 允许的"聚合（mere aggregation）"——**不会**将本项目自身的 LGPL 代码"传染"为 GPL，本项目自身代码仍按 LGPL-3.0 授权。但随包分发这些 GPL 组件时，仍须按各自许可证提供其许可证全文与对应源码获取方式（见上文第二节与 `LICENSE`）。

本文件（开源声明.md）用于履行对**上述第三方开源软件**的署名与许可证告知义务；项目自身代码的授权以 `LICENSE` 为准。
